["Hashing", [{"question_name": "Print all pairs with given sum", "question_description": "Given an array of integers and a target sum, find all pairs of elements in the array whose sum equals the target. Return all unique pairs (considering order doesn't matter). For example, given array [1, 5, 7, -1, 5] and sum = 6, the pairs are (1, 5) and (7, -1).", "question_answers": ["Use nested loops to check every possible pair combination. For each element, iterate through the rest of the array to find if any other element sums to target. Time complexity O(n\u00b2), space O(1).", "Sort the array first, then use two pointers approach. Start with pointers at both ends and move inward based on comparison with target sum. Time complexity O(n log n) for sorting, space O(1).", "Use a hash map to store elements as we iterate. For each element, check if (target - current) exists in the map. This finds pairs in linear time with O(n) space complexity.", "Use a frequency map to count occurrences of each element. For each element, calculate complement and check if it exists in the map, handling duplicates properly. Time O(n), space O(n)."], "question_answer_correct_index": 3, "question_hint": "Think about handling duplicate elements and ensuring each pair is counted only once. Consider using a frequency map to track element counts."}, {"question_name": "Longest Subsequence with Adjacent Difference of 0 or 1", "question_description": "Given an array of integers, find the length of the longest subsequence where the difference between adjacent elements is either 0 or 1. A subsequence is a sequence that can be derived from the array by deleting some or no elements without changing the order of the remaining elements. Example: For arr = [1, 2, 3, 2, 3, 4], the longest subsequence is [1, 2, 3, 2, 3, 4] with length 6.", "question_answers": ["Use recursion to try all possible subsequences, checking adjacent differences. This brute force approach explores all 2^n possibilities, making it extremely inefficient for larger arrays.", "Use dynamic programming with O(n^2) time complexity. For each element, check all previous elements and maintain longest subsequence ending at that position if difference is 0 or 1.", "Use a hash map to track the longest subsequence ending with each value. For each element, update the longest length for that value and its neighbors (value-1 and value+1) in O(1) time per element.", "Sort the array first and then use dynamic programming to find longest subsequence with adjacent difference 0 or 1. This approach reduces complexity but may not maintain original order.", "Use a segment tree or Fenwick tree to query maximum subsequence length for values in range [arr[i]-1, arr[i]+1] efficiently, achieving O(n log M) time where M is the range of values."], "question_answer_correct_index": 2, "question_hint": "Think about using a hash map to track the longest subsequence ending with each possible value. For each element, you can update not just for that value but also for its neighbors (value-1 and value+1)."}, {"question_name": "Longest Consecutive Sequence", "question_description": "Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence. You must write an algorithm that runs in O(n) time. Example: Input: nums = [100,4,200,1,3,2] Output: 4 Explanation: The longest consecutive elements sequence is [1, 2, 3, 4].", "question_answers": ["Sort the array first, then iterate through it to find consecutive sequences. This approach takes O(n log n) time due to sorting, which doesn't meet the O(n) requirement but is straightforward to implement.", "Use a hash set to store all numbers, then for each number check if it's the start of a sequence by looking for number-1. If it is, count consecutive numbers forward. This avoids sorting and runs in O(n) time.", "Use a hash map to track sequence lengths, updating boundaries when new numbers are added. For each number, check neighbors and merge sequences, maintaining the longest length found. This runs in O(n) time.", "Use a union-find (disjoint set) data structure to group consecutive numbers together. Initialize each number as its own set, then union with neighbors if they exist. This runs in O(n) time with path compression.", "Use a sliding window approach after sorting, but this still requires O(n log n) time. Maintain a window of consecutive numbers and expand when the next number is consecutive, otherwise reset the window."], "question_answer_correct_index": 1, "question_hint": "Think about how to identify the start of a sequence efficiently without sorting. A number is the start of a sequence if number-1 is not in the set."}, {"question_name": "Count Subarrays with given XOR", "question_description": "Given an array of integers and a target XOR value, count the number of subarrays whose XOR equals the target value. For example, given array [4, 2, 2, 6, 4] and target XOR = 6, the answer is 4 subarrays.", "question_answers": ["Generate all possible subarrays using nested loops, compute XOR for each subarray, and count those matching target. Time complexity O(n\u00b3) due to XOR computation for each subarray.", "Use prefix XOR array to compute XOR of subarrays in O(1) time, then use nested loops to check all subarrays. Time complexity O(n\u00b2) with O(n) space.", "Use hashmap to store prefix XOR frequencies. For each element, compute prefix XOR and check if (prefix XOR ^ target) exists in hashmap. Time complexity O(n) with O(n) space."], "question_answer_correct_index": 2, "question_hint": "Think about using prefix XOR and the property that if prefix XOR up to i is P_i and up to j is P_j, then XOR of subarray from i+1 to j is P_i ^ P_j."}]]